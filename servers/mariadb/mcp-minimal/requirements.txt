# Minimal requirements for MariaDB MCP server
# No ML/embedding packages - just database and MCP functionality

# MCP framework
# 3.2.0+ fixes CVE-2026-27124, CVE-2026-32871; also covers
# CVE-2025-69196, GHSA-c2jp-c369-7pvx, GHSA-rcfx-77hg-w2wv
fastmcp==3.2.0

# Database connectivity (async)
asyncmy>=0.2.10

# Environment configuration
python-dotenv>=1.0.0

# Core dependencies (pulled in by fastmcp but listing explicitly)
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP server for running in HTTP transport mode
uvicorn>=0.30.0

# Pin transitive deps to fix CVEs
jaraco-context>=6.1.0
wheel>=0.46.2