apiVersion: v1
kind: Service
metadata:
  name: mariadb-mcp-minimal
  namespace: mariadb
  labels:
    app: mariadb-mcp-minimal
    version: minimal
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: mariadb-mcp-minimal