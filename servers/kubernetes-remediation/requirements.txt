# Requirements for Kubernetes Remediation MCP server
# Minimal dependencies - just MCP functionality

# MCP framework (>=2.14.5 fixes GHSA-c2jp-c369-7pvx, GHSA-rcfx-77hg-w2wv, CVE-2025-66416)
fastmcp[websockets]>=2.14.5

# Environment configuration
python-dotenv==1.1.0

# HTTP server for running in HTTP transport mode
uvicorn>=0.35.0

# Pin upgraded versions to fix CVEs
cryptography>=46.0.5
jaraco-context>=6.0.0
wheel>=0.46.0
setuptools>=80.0.0
