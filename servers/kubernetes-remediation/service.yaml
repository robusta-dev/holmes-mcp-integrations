apiVersion: v1
kind: Service
metadata:
  name: kubernetes-remediation-mcp
  labels:
    app: kubernetes-remediation-mcp
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: kubernetes-remediation-mcp
